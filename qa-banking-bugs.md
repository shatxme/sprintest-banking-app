# Sprintest Bank QA Bug Index

> 🎯 **Для инструкторов**: Это полный список намеренных багов в приложении. Студентам НЕ показывать!

## Quick Stats

- **Всего багов**: 22
- **Критических** (влияют на деньги): 2
- **Высокий приоритет** (функциональные): 8
- **Средний приоритет** (UX/отображение): 8
- **Низкий приоритет** (edge cases): 4

## Категории багов

### 1. Баги валидации (Validation Bugs)

#### API-VALIDATION-001: Отсутствует проверка отрицательных сумм при переводе
- **Где**: `src/pages/api/qa-banking/transfers.ts`
- **Суть**: API эндпоинт принимает отрицательные значения `amount`, хотя бизнес-логика их отклоняет
- **Как найти**: Отправить POST запрос с `amount: -100`
- **Ожидаемое поведение**: API должен вернуть 400 ошибку на уровне валидации запроса
- **Фактическое поведение**: Валидация происходит на уровне бизнес-логики, возвращая непонятную ошибку

#### API-VALIDATION-002: Отсутствует валидация формата номера счета
- **Где**: `src/data/qa-banking.ts` в функции `createTransfer`
- **Суть**: Не проверяется формат номера счета получателя (должен быть 20 цифр)
- **Как найти**: Создать перевод на счет "123" или "abc"
- **Ожидаемое поведение**: Ошибка о неверном формате номера счета
- **Фактическое поведение**: Перевод создается с некорректным номером

#### API-VALIDATION-003: Нет проверки длины описания перевода
- **Где**: `src/data/qa-banking.ts` в функции `createTransfer`
- **Суть**: Description может быть пустой строкой или слишком длинной
- **Как найти**: Отправить пустую строку или строку 10000+ символов
- **Ожидаемое поведение**: Валидация минимальной и максимальной длины
- **Фактическое поведение**: Принимает любую строку

#### API-VALIDATION-004: Недостаточная валидация etaDays
- **Где**: `src/pages/api/qa-banking/requests.ts`
- **Суть**: API принимает 0 и отрицательные значения для `etaDays`
- **Как найти**: Создать заявку с `etaDays: 0` или `etaDays: -5`
- **Ожидаемое поведение**: Ошибка валидации
- **Фактическое поведение**: `Math.max(1, ...)` молча исправляет на 1

#### UI-VALIDATION-001: Проверка лимита только при отправке формы
- **Где**: `src/components/BankingChallengeApp.tsx` в `handleTransfer`
- **Суть**: UI не предупреждает о превышении дневного лимита до отправки
- **Как найти**: Ввести сумму больше остатка лимита и попытаться отправить
- **Ожидаемое поведение**: Предупреждение перед отправкой или блокировка кнопки
- **Фактическое поведение**: Ошибка приходит только от API

### 2. Баги расчетов (Calculation Bugs)

#### CALC-001: Неправильный расчет комиссии (критический!)
- **Где**: `src/data/qa-banking.ts` в функции `createTransfer`
- **Суть**: Используется `Math.min` вместо `Math.max` для расчета комиссии
- **Формула**: `Math.min(amount * 0.0075, 45)` вместо `Math.max(amount * 0.0075, 45)`
- **Как найти**: 
  - Перевод 1000₽ → должна быть комиссия 45₽, но будет 7.5₽
  - Перевод 10000₽ → должна быть комиссия 75₽, но будет 45₽
- **Уровень**: 🔴 Критический - банк теряет деньги!

#### CALC-002: Неправильная граница проверки овердрафта
- **Где**: `src/data/qa-banking.ts` в двух местах проверки баланса
- **Суть**: Используется `<=` вместо `<` при проверке -50000
- **Как найти**: Попробовать создать перевод, который приведет к балансу ровно -50000₽
- **Ожидаемое поведение**: Перевод должен быть разрешен
- **Фактическое поведение**: Ошибка "Недостаточно средств"

#### CALC-003: Не учитываются комиссии в расчете дневного лимита
- **Где**: `src/components/BankingChallengeApp.tsx` в `usedLimit`
- **Суть**: При подсчете использованного лимита не учитывается категория 'fee'
- **Как найти**: Сделать несколько внешних переводов, проверить Progress bar
- **Ожидаемое поведение**: Лимит должен включать и переводы, и комиссии
- **Фактическое поведение**: Показывается только сумма переводов без комиссий

#### CALC-004: Обход дневного лимита через множественные переводы
- **Где**: `src/data/qa-banking.ts` в функции `createTransfer`
- **Суть**: Проверка лимита смотрит только на сумму текущего перевода, не на накопленную
- **Как найти**: Сделать 3 перевода по 200000₽ при лимите 300000₽
- **Ожидаемое поведение**: Второй перевод должен быть отклонен
- **Фактическое поведение**: Все три проходят успешно

### 3. Баги форматирования и отображения (Display Bugs)

#### UI-DISPLAY-001: Неправильное форматирование срока действия карты
- **Где**: `src/components/BankingChallengeApp.tsx` в `formatCardExpiry`
- **Суть**: Используется `getDate()` (день месяца) вместо `getMonth() + 1`
- **Как найти**: Посмотреть на карты, срок будет "01/27" вместо правильного месяца
- **Ожидаемое поведение**: MM/YY формат
- **Фактическое поведение**: DD/YY формат (день вместо месяца)

#### UI-DISPLAY-002: Лишний пробел в номере карты
- **Где**: `src/components/BankingChallengeApp.tsx` в `formatCardNumber`
- **Суть**: Убран `.trim()`, добавляется лишний пробел в конце
- **Как найти**: Скопировать номер карты с UI
- **Ожидаемое поведение**: "5264 9200 4412 9801"
- **Фактическое поведение**: "5264 9200 4412 9801 "

#### UI-DISPLAY-003: Отсутствует год в датах транзакций
- **Где**: `src/components/BankingChallengeApp.tsx` в `formatDate`
- **Суть**: Формат даты не включает год
- **Как найти**: Посмотреть на старые транзакции из 2023 года
- **Ожидаемое поведение**: Должен показываться год для транзакций не из текущего года
- **Фактическое поведение**: Все даты без года, например "01 фев, 12:05"

#### UI-DISPLAY-004: Символ валюты всегда показывает рубли
- **Где**: `src/components/BankingChallengeApp.tsx` в форме пополнения
- **Суть**: Label показывает "(₽)" даже для USD/EUR счетов
- **Как найти**: Выбрать USD счет, открыть форму пополнения
- **Ожидаемое поведение**: Показывать символ валюты выбранного счета
- **Фактическое поведение**: Всегда "₽"

### 4. Баги состояния и данных (State & Data Bugs)

#### STATE-001: Форма пополнения не очищается после успеха
- **Где**: `src/components/BankingChallengeApp.tsx` в `handleTopUp`
- **Суть**: После успешного пополнения поля формы не очищаются
- **Как найти**: Пополнить счет, затем снова открыть форму
- **Ожидаемое поведение**: Чистая форма
- **Фактическое поведение**: Предыдущая сумма остается в поле

#### DATA-001: API возвращает неправильную валюту при пополнении
- **Где**: `src/data/qa-banking.ts` в функции `createTopUp`
- **Суть**: В транзакции всегда указывается `currency: 'RUB'` вместо `account.currency`
- **Как найти**: Пополнить USD счет, проверить ответ API
- **Ожидаемое поведение**: `"currency": "USD"`
- **Фактическое поведение**: `"currency": "RUB"`

#### DATA-002: Транзакции не отсортированы по дате
- **Где**: `src/data/qa-banking.ts` в функции `listTransactions`
- **Суть**: Нет явной сортировки по `createdAt DESC`
- **Как найти**: Создать несколько транзакций, проверить порядок в разных запросах
- **Ожидаемое поведение**: Всегда от новых к старым
- **Фактическое поведение**: Порядок может быть непредсказуемым

### 5. Баги обработки входных данных (Input Processing Bugs)

#### INPUT-001: Некорректная обработка множественных запятых
- **Где**: `src/components/BankingChallengeApp.tsx` в `parseAmount`
- **Суть**: `replace(",", ".")` заменяет только первую запятую
- **Как найти**: Ввести "1,000,50" в поле суммы
- **Ожидаемое поведение**: Ошибка или правильный парсинг
- **Фактическое поведение**: Парсится как "1.000,50" что даст неверный результат

### 6. Прочие логические баги (Logic Bugs)

#### LOGIC-001: Неправильный counterparty при переводе между своими счетами
- **Где**: `src/data/qa-banking.ts` в функции `createTransfer`
- **Суть**: При переводе между собственными счетами в разных валютах показывается одинаковое имя
- **Как найти**: Перевести с RUB счета на USD счет одного владельца
- **Ожидаемое поведение**: Должны быть разные описания или упоминание валюты
- **Фактическое поведение**: Оба показывают одинаковое имя держателя

#### LOGIC-002: Проблема с расчетом estimatedReadyAt через границы месяцев
- **Где**: `src/data/qa-banking.ts` в функции `createProductRequest`
- **Суть**: `setDate` может некорректно работать при переходе через границы месяцев
- **Как найти**: Создать заявку 31 января с etaDays: 31
- **Ожидаемое поведение**: Правильная дата в марте
- **Фактическое поведение**: Может быть февраль или март в зависимости от года

## Как проверить ключевые баги

### Commission Bug (CALC-001)
```bash
# Тест: малая сумма
curl -X POST http://localhost:4321/api/qa-banking/transfers \
  -H "Content-Type: application/json" \
  -d '{"fromAccountId": "acc-001", "toAccountNumber": "40702810000050000001", "amount": 1000}'

# Ожидаемая комиссия: 45 RUB (минимум)
# Фактическая комиссия: 7.5 RUB (0.75% от 1000)
```

### Daily Limit Bypass (CALC-004)
```bash
# Сделать 3 перевода по 200k при лимите 300k
# Все должны пройти, хотя не должны!

# Перевод 1
curl -X POST http://localhost:4321/api/qa-banking/transfers \
  -H "Content-Type: application/json" \
  -d '{"fromAccountId": "acc-001", "toAccountNumber": "40702810000050000001", "amount": 200000}'

# Повторить 3 раза - все пройдут успешно!
```

### Currency Bug (DATA-001)
```bash
# Пополнить USD счет
curl -X POST http://localhost:4321/api/qa-banking/topups \
  -H "Content-Type: application/json" \
  -d '{"accountId": "acc-003", "amount": 100}'

# Ответ покажет "currency": "RUB" вместо "USD"
```

## Рекомендации для тестирования

### Обязательные тест-кейсы
1. ✅ Комиссии при переводах (малые и большие суммы)
2. ✅ Дневной лимит переводов (одна и несколько операций)
3. ✅ Операции с разными валютами
4. ✅ Граничные значения (0, отрицательные, очень большие)
5. ✅ Форматирование дат и номеров карт
6. ✅ Состояние форм после операций
7. ✅ Сравнение данных UI vs API

### Инструменты
- curl для API тестирования
- DevTools браузера (F12) для проверки запросов
- Калькулятор для проверки расчетов

## Рекомендуемая система оценки

| Найдено багов | Оценка | Комментарий |
|---------------|--------|-------------|
| 0-3 | D | Только базовая проверка |
| 4-8 | C | Найдены очевидные UI баги |
| 9-14 | B | Хорошее функциональное тестирование |
| 15-19 | A | Отличный систематический подход |
| 20-22 | A+ | Выдающийся результат, найдены edge cases |

**Дополнительные баллы за:**
- Хорошо документированные баг-репорты с шагами воспроизведения
- Классификация по серьезности/приоритету
- Скриншоты/видео багов
- Предложения по исправлению

## Что должны сдать студенты

1. **Bug Report** (Excel/Google Sheets)
   - Bug ID
   - Заголовок
   - Серьезность/Приоритет
   - Шаги воспроизведения
   - Ожидаемый vs Фактический результат
   - Скриншот
   
2. **Test Cases** (минимум 20-30 тест-кейсов)
   - Test ID
   - Описание теста
   - Предусловия
   - Шаги теста
   - Ожидаемый результат
   
3. **Итоговый отчет** (executive summary)

## Частые ошибки студентов

❌ **Не сравнивают UI vs API** - Многие баги требуют проверки обоих
❌ **Тестируют только happy paths** - Большинство багов в edge cases
❌ **Не используют калькулятор** - CALC-001 легко пропустить без проверки математики
❌ **Сдаются слишком рано** - Нужно поощрять систематическое boundary testing
❌ **Плохие баг-репорты** - Учите их писать четкие шаги воспроизведения

## Подсказки по неделям (для постепенного направления)

💡 **Неделя 1**: "Проверьте, правильно ли рассчитывается комиссия"
💡 **Неделя 2**: "Что происходит при очень маленьких переводах?"
💡 **Неделя 3**: "Можно ли сделать дневной лимит больше, чем он есть?"
💡 **Неделя 4**: "Проверьте все валюты, не только рубли"

> 📝 **Примечание**: Эти баги добавлены специально для обучения. Студенты должны найти их самостоятельно через exploratory testing, boundary value analysis, и API testing.
